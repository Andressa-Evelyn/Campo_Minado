package CampoMinado;

public class explosaoException extends RuntimeException {
    private static final long serialVersionUID = 1L;

    // A serialização é usada quando você deseja salvar o estado de um objeto para que possa ser recriado mais tarde, ou quando você deseja enviar o objeto através de uma rede para outro sistema.

    // Por exemplo, se você tiver um objeto que representa um usuário com propriedades como nome, idade e endereço, você pode serializar esse objeto para um formato como JSON ou XML, que pode ser salvo em um arquivo ou enviado através de uma rede. Mais tarde, você pode desserializar esse JSON ou XML de volta para um objeto em seu programa.
}
